# MongoDB connection string
MONGO_URI=<your-connection-string-here>

# Secret used to sign JWT tokens (use a strong random value in production)
JWT_SECRET=your_jwt_secret_here

# Default admin credentials (only created if no admin exists)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Optional port
PORT=5000
